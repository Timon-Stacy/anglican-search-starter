# Data + build artifacts (too large / machine-specific for git)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.faiss
*.faiss.bak
*.bak

# Python env / caches
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
uv_sync.log
mcp_server.err

# Model / HF caches (if ever placed in-tree)
hf-cache/
models/

# Downloaded source corpora (CCEL ThML, etc.) — fetched on demand, not vendored
ccel_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local agent config / instructions — never publish
.claude/
CLAUDE.md

# Secrets / local deploy config (keep only the *.example files)
.env
**/.env
**/wireguard/wg0.conf
*.key
*.pub
