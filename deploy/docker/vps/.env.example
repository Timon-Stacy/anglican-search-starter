# VPS edge config. Copy to `.env` and fill in. Never commit the real .env.

# Your domain (A record must point at this VPS's public IP). Caddy issues the
# Let's Encrypt cert for it automatically.
DOMAIN=library.example.com
# Email for Let's Encrypt expiry notices.
ACME_EMAIL=you@example.com

TZ=Etc/UTC
